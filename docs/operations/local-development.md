# Local Development

The repository currently contains only its architecture scaffold. Component
prerequisites and runnable commands will be documented when the Flutter app,
Go API, and Docker services are initialized. No application or service can be
started yet. GNU Make is optional; `make help` only prints the scaffold status.
