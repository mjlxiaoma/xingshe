# User Flows

## Core trip flow

```text
Open app -> browse map or spots -> choose a spot -> start trip
-> record route and photos -> finish trip -> review -> share locally
```

Permissions are requested at the point of use. Ending a trip stops foreground
location recording.
